export const getNotes = (state) => state.data.notes;
export const getNoteFormData = (state) => state.data.noteForm;
export const getCurrentNote = (state) => state.data.currentNote;
export const getLang = (state) => state.data.lang;
